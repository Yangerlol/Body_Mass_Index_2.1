# ⚖️ Проект: Расчёт индекса массы тела (Body Mass Index)

## 📝 Описание проекта
Приложение рассчитывает индекс массы тела (BMI) на основе веса (в килограммах) и роста (в метрах) пользователя. Логика расчета вынесена в отдельный сервис.

---

## 🛠 Технологический стек
* **Язык программирования:** Java 11
* **Архитектура:** Service-oriented approach (BmiService)
* **Среда разработки:** IntelliJ IDEA

---

## ⚙️ Условия задачи
* **Формула:** Вес (кг) разделить на квадрат роста (м).
* **Реализация:** Метод `calculate` принимает вес как `int` и рост как `double`, возвращая целое число (индекс).

---

## 🚀 Как запустить проект
1. Склонируйте репозиторий:
   `git clone https://github.com/Yangerlol/Body_Mass_Index_2.1.git`
2. Откройте проект в IntelliJ IDEA.
3. Запустите класс `Main` для проверки работы сервиса.

---

## 📊 Пример реализации кода

**BmiService.java**
```java
public class BmiService {
    public int calculate(double heightInMeters, int weightInKilograms) {
        double bmi = weightInKilograms / (heightInMeters * heightInMeters);
        return (int) bmi;
    }
}
```
**Main.java**
```java
public class Main {
    public static void main(String[] args) {
        BmiService service = new BmiService();
        double height = 1.87;
        int weight = 98;
        int bmi = service.calculate(height, weight);
        System.out.println("Ваш индекс массы тела: " + bmi);
    }
}
```
## ✅ Результаты работы
В ходе выполнения проекта реализовано:

Инкапсуляция логики: расчет вынесен в специальный класс BmiService.

Работа с типами данных: корректное использование double для роста и приведение результата к int.

Математические операции: программная реализация возведения в квадрат и деления.
